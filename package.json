{
  "scripts": {
    "serve": "pm2 start server.js --watch --no-daemon || node server.js",
    "test": "node `yarn bin`/standard"
  },
  "dependencies": {
    "express": "^4.17.1",
    "phaser": "^3.21.0",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "standard": "*"
  }
}
