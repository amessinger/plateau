{
  "scripts": {
    "serve": "pm2 start server.js --watch --no-daemon || node server.js",
    "test": "node `yarn bin`/standard"
  },
  "dependencies": {
    "express": "^4.17.3",
    "phaser": "^3.21.0",
    "socket.io": "^2.4.0"
  },
  "devDependencies": {
    "standard": "*"
  }
}
